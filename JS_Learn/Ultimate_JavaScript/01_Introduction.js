console.log("This program adds 1, 2, and 27");      // This program adds 1, 2, and 27

function AddThreeNumbers(a, b, c) {
    return a+b+c
}
let c = AddThreeNumbers(1, 2, 27);

console.log(c);       // 30