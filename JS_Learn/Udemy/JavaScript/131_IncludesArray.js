const guestList = ["Ram", "Sam", "Hum", "Lax", "bivi", "Jam"];

const guestName = "Lss";

if (guestList.includes(guestName)) {
  console.log("Welcome!");
} else {
  console.log("Maybe next time.");
}

/**
 * var guestList = ["Ram", "Sam", "Hum", "Lax", "bivi", "Jam"];
 *
 * var guestName = prompt("What's your name?");
 *
 * if (guestList.includes(guestName)) {
 *    alert("Welcome!");
 * } else {
 *   alert("Maybe next time.");
 * }
 */
